<span class="reputation-info" style="<%= style ||= '' %>">
<% if participant.admin %>
  <span class="reputation-score" data-toggle="tooltip" title="Admin">
    ###
  </span>
<% elsif current_participant.admin %>
  <span class="reputation-score" data-toggle="tooltip" title="<%= participant.final_temporary_rating %> ± <%= participant.temporary_variation.to_i %>">
    <% if defined?(full) && full %>
      <%= participant.final_temporary_rating %> ± <%= participant.temporary_variation.to_i %>
    <% else %>
      <%= participant.final_temporary_rating %>
    <% end %>
  </span>
<% end %>
</span>
