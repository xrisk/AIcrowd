<div class="btn-set" aria-hidden="true">
	<%= f.button (f.object.new_record? ? "Create challenge" : "Update challenge"), name: 'current_step', value: current_step, class: 'btn btn-primary btn-sm challenge-edit-submit', data: { disable_with: 'Saving...', controller: 'challenges', action: 'click->challenges#alert_for_new_rules' } %>
	<%= link_to 'Cancel', challenge_path(f.object), class: 'btn btn-secondary' if f.object.persisted? %>
</div>
