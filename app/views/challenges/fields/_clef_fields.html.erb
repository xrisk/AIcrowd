<label>CLEF Task</label>
<%= collection_select(
      :challenge,
      :clef_task_id,
      ClefTask.where(organizer: organizers),
      :id,
      :task,
      include_blank: false,
      prompt: 'No task selected') %>
