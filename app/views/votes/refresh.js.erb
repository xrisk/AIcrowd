function handleVoteButtonRefresh() {
  const voteLinkId       = "#<%= "#{votes_link_id(@votable)}" %>";
  const refreshedContent = "<%= j render 'shared/votes/button', votable: @votable, vote: @vote %>";

  $(voteLinkId).replaceWith(refreshedContent);
}

handleVoteButtonRefresh();
