namespace :review do
  desc 'Bootstrap a review app when it is provisioned'
  task bootstrap: :environment do
    # TODO
    # Seed database
    # Fix env variables like DOMAIN_NAME based on HEROKU_APP_NAME
    # Fix integrations
  end
end
