<%= concept(Submission::Cell::TableRow,
                      collection: @submissions,
                      challenge: @challenge,
                      current_round: @current_round,
                      current_participant: current_participant) %>