var temp = $(document).height();

$("#pc-ul").append("<%= j render partial: 'participant_challenge_rows' %>");
$('#load_more_link').replaceWith("<%= j render partial: 'shared/load_more', locals: { collection: @participant_challenges }, :id=>'load_more_link' %>");

$('html, body').animate({
   scrollTop: 2*temp - $(document).height()},
   1400,
   "easeOutQuint"
);
